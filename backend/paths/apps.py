"""App configuration for the paths app."""

from django.apps import AppConfig


class PathsConfig(AppConfig):
    """Configuration for the paths application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "paths"
