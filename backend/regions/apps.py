"""App configuration for the regions app."""

from django.apps import AppConfig


class RegionsConfig(AppConfig):
    """Configuration for the regions application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "regions"
