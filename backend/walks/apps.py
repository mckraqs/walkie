"""App configuration for walks."""

from django.apps import AppConfig


class WalksConfig(AppConfig):
    """Configuration for the walks app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "walks"
