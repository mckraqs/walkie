# Settings

## Theme Toggle

Walkie supports light and dark themes. Click the theme toggle icon (sun/moon) in the
top-right corner of any page to switch between:

- **Light mode** - a bright interface with light backgrounds
- **Dark mode** - a darker interface that is easier on the eyes in low light

The theme preference follows your system setting by default and persists across
sessions.
